Hello,

You have signed up for <%= $site_name %>, with the user name of <%= @user.username %>.

Please click the following link (or copy it into your browser's url bar) to verify your email and activate your account.

<%= edit_verification_user_url(@user,
  :code => @user.verify_code,
  :host => $site_url,
  :protocol => Azimux::AxUser.protocol("https")
) %>

Thank you.