Hello <%= @user.username.to_s %>,

You have requested to reset your password for <%= $site_name %>

Please click the following link (or copy it into your browser's url bar) to enter a new password.

<%= edit_password_user_url(@user,
  :code => @user.password_reset_request.code,
  :host => $site_url,
  :protocol => Azimux::AxUser.protocol($email_protocol || "http")
)
%>

If you did not request a password reset, please ignore this email.

Thank you.